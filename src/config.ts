// ===== 遊戲常數 =====

// 畫面尺寸
export const GAME_WIDTH = 800;
export const GAME_HEIGHT = 600;

// 棋盤設定
export const COLS = 10;          // 棋盤寬度（10 格）
export const ROWS = 20;          // 棋盤高度（20 格）
export const BLOCK_SIZE = 30;    // 每格的像素大小

// 下落速度
export const DROP_INTERVAL = 1000;      // 正常下落間隔（ms）
export const FAST_DROP_INTERVAL = 25;   // 加速下落間隔（ms）

// 移動設定
export const MOVE_DELAY = 200;          // 首次按住後延遲（ms）
export const MOVE_INTERVAL = 250;       // 連續移動間隔（ms）
